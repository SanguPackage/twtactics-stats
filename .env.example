# Copy to .env (git-ignored) for local `docker compose up`.
# In production these are injected by Coolify (set via the Home repo coolify-apps.ts manifest).
TWSTATS_DB_PASSWORD=TheSanguPackageCheaperHosting
TWSTATS_ROOT_PASSWORD=change-me-in-coolify
